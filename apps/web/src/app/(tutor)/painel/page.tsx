import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Painel' };

export default function TutorPainelPage() {
  return (
    <main>
      <h1>Painel do tutor</h1>
      {/* TODO: upcoming appointments, pet list, quick actions */}
    </main>
  );
}
