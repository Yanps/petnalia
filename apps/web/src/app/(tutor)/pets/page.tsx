import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Meus pets' };

export default function PetsPage() {
  return (
    <main>
      <h1>Meus pets</h1>
      {/* TODO: PetProfileCard list, add pet CTA */}
    </main>
  );
}
