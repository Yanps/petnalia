import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Minhas consultas' };

export default function TutorConsultasPage() {
  return (
    <main>
      <h1>Minhas consultas</h1>
      {/* TODO: AppointmentCard list, filters by status */}
    </main>
  );
}
