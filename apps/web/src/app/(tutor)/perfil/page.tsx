import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Meu perfil' };

export default function TutorPerfilPage() {
  return (
    <main>
      <h1>Meu perfil</h1>
      {/* TODO: ProfileForm (nome, telefone, endereço) */}
    </main>
  );
}
