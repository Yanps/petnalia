import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Consultas — Admin' };

export default function AdminConsultasPage() {
  return (
    <main>
      <h1>Consultas</h1>
      {/* TODO: all appointments table, dispute management */}
    </main>
  );
}
