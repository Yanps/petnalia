import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Perfil — Veterinário' };

export default function VetPerfilPage() {
  return (
    <main>
      <h1>Perfil do veterinário</h1>
      {/* TODO: profile form, specialties, photo upload, CRMV */}
    </main>
  );
}
