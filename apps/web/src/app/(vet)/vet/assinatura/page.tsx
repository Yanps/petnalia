import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Assinatura' };

export default function AssinaturaPage() {
  return (
    <main>
      <h1>Assinatura</h1>
      {/* TODO: subscription plan cards, current plan status */}
    </main>
  );
}
