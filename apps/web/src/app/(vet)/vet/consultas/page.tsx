import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Consultas — Veterinário' };

export default function VetConsultasPage() {
  return (
    <main>
      <h1>Consultas</h1>
      {/* TODO: AppointmentCard list, status filters, AppointmentTimeline */}
    </main>
  );
}
