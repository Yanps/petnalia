import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Área de atendimento' };

export default function AreaAtendimentoPage() {
  return (
    <main>
      <h1>Área de atendimento</h1>
      {/* TODO: RadiusSelector + CoverageMap (Mapbox) */}
    </main>
  );
}
