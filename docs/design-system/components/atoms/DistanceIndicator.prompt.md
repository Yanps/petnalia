DistanceIndicator — map-pin + distance from the user's saved address. Used on vet cards and address previews.

```jsx
<DistanceIndicator km={2.4} />
<DistanceIndicator km={5} label="" size="md" />
```
