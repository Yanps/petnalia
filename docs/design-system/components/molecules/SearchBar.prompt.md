SearchBar — the marketplace search molecule: Address Input + Pet Type Select + Specialty Select + Search Button, in a single elevated pill.

```jsx
<SearchBar onSearch={runSearch} />
```

Presentational scaffold — wire each control to your search state. On mobile, collapse to a single tappable field that opens a full-screen search Sheet.
