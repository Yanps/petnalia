Label — accessible field label (maps to shadcn/ui Label, built on Radix Label). Always wire `htmlFor` to the control id.

```jsx
<Label htmlFor="cep" required>CEP</Label>
<input id="cep" className="vn-input" />
```
