Checkbox — labelled checkbox for multi-select filters and consent (maps to shadcn/ui Checkbox).

```jsx
<Checkbox label="Home visit available" defaultChecked />
<Checkbox label="Accepts new patients" />
```

Props: `label`, plus native input attrs (`checked`, `defaultChecked`, `disabled`, `onChange`).
