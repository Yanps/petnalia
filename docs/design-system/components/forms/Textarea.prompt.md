Textarea — multi-line input with label/hint/error (maps to shadcn/ui Textarea). Vertically resizable, 96px min height.

```jsx
<Textarea label="Observações" placeholder="Conte ao vet sobre os sintomas…" rows={4} />
```
