Switch — instant-apply on/off toggle (maps to shadcn/ui Switch). Use for settings that take effect immediately; use Checkbox inside forms that are submitted.

```jsx
<Switch label="Online consultations" defaultChecked />
<Switch label="SMS reminders" />
```
