Tooltip — short hover/focus label above an interactive trigger (maps to shadcn/ui Tooltip). Reveals on `:hover` and `:focus-within` for keyboard access.

```jsx
<Tooltip label="Verified veterinarian"><Icon name="shield-check" /></Tooltip>
```

Keep to a few words; never put essential-only info here. Wraps exactly one trigger child.
