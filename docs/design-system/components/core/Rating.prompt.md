Rating — compact star rating used on vet cards and profiles.

```jsx
<Rating value={4.9} count={213} />
```

Shows numeric average + a single filled star + review count, the marketplace-standard compact form. Props: `value` (0–5), `count`, `showValue`, `size`.
