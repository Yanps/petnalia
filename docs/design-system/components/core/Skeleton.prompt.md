Skeleton — shimmering loading placeholder. Mirror the shape of the content it stands in for (avatar circle + text lines). Animation auto-disables under prefers-reduced-motion.

```jsx
<Skeleton circle height={48} />
<Skeleton width={180} height={20} />
<Skeleton width="60%" height={14} />
```
